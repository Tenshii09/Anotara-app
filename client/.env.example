# Local development defaults (copied to .env or .env.local).
VITE_API_BASE_URL=http://127.0.0.1:5000

# Production API URL lives in .env.production (used by `npm run build`).
# VITE_API_BASE_URL=http://13.239.232.218:5000

# Mapbox / Firebase keys — see client/.env for full list.
